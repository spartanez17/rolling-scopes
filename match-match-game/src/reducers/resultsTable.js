import { ADD_RESULT } from '../actions'


const resultsTable = (state = [], action) => {
    switch (action.type) {
        case ADD_RESULT:
            nextState = state;
            if(nextState.){

            }
        default:
            return state
    }
}

export default profile;
